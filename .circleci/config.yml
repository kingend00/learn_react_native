version: 4
jobs:
    build:
        docker:
            - image: yarnpkg/node-yarn
        branches:
            only:
                - master
        steps:
            - checkout
            - run:
                name: Greeting
                command: echo this is demo CircleCI