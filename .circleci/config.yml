version: 4
jobs:
    build:
        docker:
            - image: yarnpkg/node-yarn
        branches:
            only:
                - master123
        steps:
            - checkout
            - run:
                name: Greeting
                command: echo this is demo CircleCI
                command: npm install
                command: yarn install